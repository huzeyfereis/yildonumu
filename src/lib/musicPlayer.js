// Populated by BackgroundMusic once the YouTube player is ready.
// Lets other components (e.g. the hero's scroll button) start playback
// from within their own click handler, so it counts as a user gesture.
export const musicControls = {
  play: () => {},
}
